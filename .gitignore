/target
/node_modules
/build
/.dart_tool
/coverage
.DS_Store
.env
.env.*
!.env.example
*.sqlite
Cargo.lock

# ores-compose / IndieBuild local runtime material. These directories contain
# fetched source trees, build outputs, job logs and durable report intents; they
# are operator runtime state and must never become repository policy.
/.ores/

# Repository-local temporary data and authorized Git worktrees
tmp
temp
tmp/worktrees
temp/worktrees

# Local temporary directories and repository-owned worktrees
tmp/
temp/
